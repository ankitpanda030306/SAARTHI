package com.example.dropout.util;

import com.example.dropout.entity.Student;
import org.springframework.stereotype.Component;

@Component
public class MLClient {

    public String predictRisk(Student student) {
        // This is a placeholder file from your teammate.
        // We are NOT using this anymore. We use PredictionService.java instead.
        return "Low"; 
    }
}